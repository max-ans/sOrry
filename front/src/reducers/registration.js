const initialState = {
  email: '',
  password: '',
  firstname: '',
  lastname: '',
  nickname: '',
};

const registration = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default registration;
